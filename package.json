{
	"name": "responsive-visibility",
	"version": "1.2.0",
	"description": "Example block scaffolded with Create Block tool.",
	"author": "The WordPress Contributors",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "wp-scripts build",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "npm run zip",
		"ship": "bash scripts/publish.sh",
		"release": "bash scripts/release.sh",
		"release:check": "npm run build && php -l responsive-visibility.php && find includes -name '*.php' -print0 | xargs -0 -n1 php -l && npm run zip",
		"zip": "npm run build && rm -rf responsive-visibility responsive-visibility.zip responsive-visibility-v.$npm_package_version.zip && rsync -a --exclude-from=.buildignore . responsive-visibility && zip -qr responsive-visibility-v.$npm_package_version.zip responsive-visibility",
		"start": "wp-scripts start"
	},
	"devDependencies": {
		"@wordpress/scripts": "^27.9.0"
	},
	"dependencies": {
		"classnames": "^2.5.1"
	}
}
